import { Preset } from '@pandacss/dev';

interface PresetOptions {
    /**
     * @default 'neutral'
     */
    accentColor?: AccentColor;
    /**
     * @default 'neutral'
     */
    grayColor?: GrayColor;
    /**
     * @default 'sm'
     */
    borderRadius?: BorderRadius;
    /**
     * Defines additional colors to be included in the preset. Use '*' to include all colors.
     * By default, only the gray and accent colors are included.
     * @default [] - This means no additional colors are included unless specified.
     */
    additionalColors?: Color[] | (string[] & {});
}
type Color = AccentColor | GrayColor;
type BorderRadius = (typeof borderRadii)[number];
declare const borderRadii: readonly ["none", "xs", "sm", "md", "lg", "xl", "2xl"];
type AccentColor = (typeof accentColors)[number];
declare const accentColors: readonly ["neutral", "tomato", "red", "ruby", "crimson", "pink", "plum", "purple", "violet", "iris", "indigo", "blue", "cyan", "teal", "jade", "green", "grass", "bronze", "gold", "brown", "orange", "amber", "yellow", "lime", "mint", "sky"];
type GrayColor = (typeof grayColors)[number];
declare const grayColors: readonly ["neutral", "mauve", "olive", "sage", "sand", "slate"];

declare const createPreset: (options?: PresetOptions) => Preset;
declare const defaultPreset: Preset;

export { type AccentColor, type BorderRadius, type GrayColor, type PresetOptions, accentColors, borderRadii, createPreset, defaultPreset as default, grayColors };
